# 베이스이미지
FROM node

# 명령 실행 경로 미지정시 루트
WORKDIR /app

# 외부경로 내부경로 순
# 외부경로는 . 입력시 실행 위치 전체 복제
# 내부경로 
# 실행경로의 전체 파일을 컨테이너 내부 /app 으로 복제(실행 경로로)
# COPY . ./
# 위 아래 같은 결과이나 명시적으로 바로 확인 가능하므로 아래가 좋을 수 있음
COPY . /app 

# 이미지 생성될때 실행
RUN npm install

# 포트 노출 설정
EXPOSE 80

#컨테이너 시작될때 실행
CMD ["node" , "server.js"]
